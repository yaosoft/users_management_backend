fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    firewall_name: main
    user_class: App\Entity\User
    from_email:
        address: "info@diamta.com"
        sender_name: "diamta.com"
    service:
        mailer: app.mymailer
    registration:
        confirmation:
            enabled: true